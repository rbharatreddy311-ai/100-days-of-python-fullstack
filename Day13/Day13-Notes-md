'''
                                                                          Day 13
                                                                          ___ __


passing a value
_______ _ _____

eg:

def some(a):
    for i in a :
        print(i)
some([1,2,3])

eg2:
    
a=(1,2,3)
def some(a):
    for i in a :
        print(i)
some(a)

return keyword:
______ _______

--> ina a function if a return is executed then it will exit from the function with certain returned values.

eg:

def myfunc_(b):
    return 5+b
a=myfunc_(10)
c=myfunc_(100)
print(a)
print(c)

import builtins

builtin_functions=[
    name for name in dir(builtins)
    if callable(getattr(builtins,name))]
print(builtin_functions)
print(f"Total built in functions are{len(builtin_functions)}")


Recursive functions
_________ _________

--> recursive function that calls itself repeatedly until specified condition
is met.

syntax: def func_name(parameter)
            if condition:--> base case
                return statement
            else:
                return statement
        print(func_name(arguments))



def func_name(num):
    if num==1:
        return 1
    else :
        return num*func_name(num-1)

num=5
print(func_name(num))

'''






















 
